{% block _media_castings_widget %}
    {# Définition du champ annonce_image, la variable form correspond au hamp annonce_image #}
    <p>Ici, vous pouvez ajouter vos propres images</p>
    {{form_widget(form)}}
    <input type="hidden" id="widgets-counter" value="0">
    <div class="form-group">
        <button type="button" id="add-castings" class="btn btn-primary">Ajouter une image</button>
    </div> 

{% endblock %}

{% block _media_castings_entry_row %}
    {{form_widget(form)}}
{% endblock %}

{% block _media_castings_entry_widget %}
    <div class="form-group my-2" id="block_{{id}}">
        <div class="row">
            <div class="col-10">
                <div class="row">
                    <div class="col">
                        {{form_widget(form.actor)}}
                        {{form_errors(form.actor)}}
                    </div>
                    <div class="col">
                        {{form_widget(form.role)}}
                        {{form_errors(form.role)}}</div>
                </div>
            </div>
            <div class="col-2">
                <button type="button" class="btn btn-danger" data-action="delete" data-target="#block_{{id}}">X</button>
            </div>
        </div>
    </div>
{% endblock %}